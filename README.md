The code submission for module 1's challenge.

This project was to make the preexisting code more accessible.

Non-semantic HTML elements changed.
HTML code followed a logical structure.
Contains img elements.
Added accessible alt attributes. 
Contains sequentially organized heading elements. 
Contains a concise and descriptive title. 
